version: 0.0.{build}-{branch}
branches: 
  only: 
    - master

shallow_clone: true
skip_tags: true

image:
  - Visual Studio 2019

platform:
  - x86
  - x64

configuration:
  - Debug
  - Release

install:
  - git clone --recursive https://github.com/google/googletest.git libs/gtest

build:
  parallel: true
  project: solar.sln